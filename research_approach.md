# Yorùbá OCR — Research Approach

This file is intentionally reset for a fresh experiment run.

Regenerate it after the active benchmark has completed:

```bash
python scripts/23_write_research_approach.py --output research_approach.md
```

The active benchmark is Base PaddleOCR English-pretrained recognition, PaddleOCR-VL-1.6 zero-shot, GLM-OCR zero-shot, and optional PaddleOCR-VL-1.6 SFT. Stale pilot results from older systems are not citable unless rerun through the current pipeline.
