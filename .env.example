# Copy to .env and set values locally. Never commit .env.
# A token is optional for public downloads and required only for gated assets or uploads.
HF_TOKEN=
HF_HOME=.hf_cache
HF_TRUST_REMOTE_CODE=1
WANDB_MODE=disabled
