# Core OCR framework
paddlepaddle>=2.6.0,<2.7.0          # install paddlepaddle-gpu for GPU training
paddleocr>=2.7.3,<2.8.0

# Numerics and data
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
Pillow>=10.0.0,<11.0.0
scikit-image>=0.22.0,<0.23.0         # skimage — required by PaddleOCR training (ppocr.data)

# PaddleOCR training (tools/train.py) — mirrors PaddleOCR/requirements.txt
albumentations>=1.3.0,<1.4.0
albucore>=0.0.13,<0.1.0
shapely>=2.0.0,<2.1.0
pyclipper>=1.3.0,<1.4.0
lmdb>=1.4.0,<1.5.0
rapidfuzz>=3.0.0,<4.0.0
# opencv-python>=4.8.0,<4.9.0         # installed by paddleocr
cython>=3.0.0,<3.1.0
requests>=2.28.0,<3.0.0

# Edit distance for CER/WER/DER
editdistance>=0.6.2

# Progress and logging
tqdm>=4.65.0,<5.0.0
# wandb>=0.16.0,<0.17.0               # optional: set WANDB_MODE=disabled to skip

# Config serialisation
PyYAML>=6.0,<7.0

# Visualisation (for analysis script)
matplotlib>=3.7.0,<3.8.0
seaborn>=0.12.0,<0.13.0

# ── VLM baselines/SFT: PaddleOCR-VL-1.6 and GLM-OCR ─────────────────────────
transformers>=5.0.0
accelerate>=1.1.0
# torch>=2.2.0               # install manually with correct CUDA version
# bitsandbytes>=0.43.0       # optional: 4-bit quantisation on smaller GPUs

# ── PaddleOCR-VL-1.6 (HF): export/train/eval scripts
torchvision>=0.16.0          # Required by HF AutoImageProcessor / GLM AutoProcessor
einops                       # Required by HF model definition for PaddleOCR-VL-1.6
