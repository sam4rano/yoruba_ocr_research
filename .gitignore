# Python
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Secrets — never commit
.env
.env.*
!.env.example

# Datasets & processed assets (rebuild locally or from Drive/zip)
data/raw/**
!data/raw/**/
!data/raw/**/.gitkeep

data/processed/**
!data/processed/**/
!data/processed/**/.gitkeep

data/hf_export/

# Checkpoints & training runs (keep only placeholder .gitkeep dirs if present)
experiments/**
!experiments/**/
!experiments/**/.gitkeep

# Weight / archive blobs (also covered under experiments/, but catch strays)
*.pdparams
*.pdopt
*.states
*.tar
*.tar.gz
*.zip

# PaddleOCR clone — install per machine / Colab; do not vendor
PaddleOCR/

wandb/

# OS/editor
.DS_Store
Thumbs.db
.hf_cache/

# Pre-anonymization figure backups (may contain deployment URLs)
**/ocr_extraction_interface_original.jpeg

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.synctex.gz
*.fdb_latexmk
*.fls
FormattingGuidelines-IJCAI-ECAI-26/*.pdf

# Colab/Drive backup mirrors (use scripts/shell/phase_99_backup.sh → Drive)
drive_results/

# Notebook checkpoints
.ipynb_checkpoints/
